---
import { getEntry, render } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';
import PdfEmbed from '../components/PdfEmbed.astro';

const page = await getEntry('pages', 'sponsorship-pack');
if (!page) throw new Error('Missing content/pages/sponsorship-pack.md');
const { Content } = await render(page);

const base = import.meta.env.BASE_URL.replace(/\/$/, '');
const pdfHref = `${base}/docs/awsugnz-sponsorship-pack-2026.pdf`;
---

<BaseLayout
  title={page.data.title}
  description={page.data.description}
  ogImage={page.data.ogImage ?? 'sponsors.png'}
>
  <PdfEmbed
    title={page.data.title}
    lede={page.data.lede}
    pdfHref={pdfHref}
    iframeTitle="AWSUG Aotearoa sponsorship pack 2026 (PDF)"
    downloadLabel="Open or download sponsorship pack (PDF)"
  >
    <Content />
  </PdfEmbed>
</BaseLayout>
